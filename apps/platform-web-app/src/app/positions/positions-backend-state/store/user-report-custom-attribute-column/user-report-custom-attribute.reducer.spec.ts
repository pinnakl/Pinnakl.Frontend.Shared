import { initialState, reducer } from './user-report-custom-attribute.reducer';

describe('UserReportCustomAttribute Reducer', () => {
  describe('unknown action', () => {
    it('should return the initial state', () => {
      const action = {} as any;

      const result = reducer(initialState, action);

      expect(result).toBe(initialState);
    });
  });
});
